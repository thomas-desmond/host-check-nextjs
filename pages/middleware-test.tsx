
export default function Home() {
    return (
        <main>
            Should not see this content, we should have been redirected to index page with url success parameter
        </main>
    );
}
